import React, { useState } from "react";

export default function BitcoinData() {
    console [bitcoinData, setBitcoinData] = useState();

    return (
        <div>
            {bitcoinData && ""}
        </div>
    )
}